# BuzzBlog 画像生成用 APIキー（プレースホルダ）
# 値を自分のキーに置き換え、プロジェクトルートに「.env」という名前で保存してください。
# このファイルは公開リポジトリにコミットしないこと（.env は .gitignore 済み）。

# OpenAI（gpt-image-2：サムネ・図解）。「GPTのAPIキー」もこれと同じ1本です。
OPENAI_API_KEY=sk-REPLACE_ME
OPENAI_IMAGE_MODEL=gpt-image-2

# Google Gemini（gemini-3-pro-image-preview：挿絵・画像のフォールバック）
GEMINI_API_KEY=AIza-REPLACE_ME
